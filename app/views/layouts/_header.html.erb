<nav class="navbar navbar-fluid-top" role="navigation">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="<%= root_path %>">INICIO <i class="icon-home"></i> </a>
      <ul class="brand">
            <b><%= content_for?(:header) ? yield(:header) : " " %></b>
      </ul>
       <ul class="nav pull-right">
          <% if signed_in? %>
            <div class="btn-group">
              <a class="btn " href="#"><i class="icon-user"></i><%= current_user.name %></a>
              <a class="btn dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to glyph(:"eye-open") + " Perfil", current_user %></li>
                <li><%= link_to glyph(:pencil) + " Actualizar Perfil", edit_user_path(current_user) %></li>
                <li></li>
                <li class="divider"></li>
                <li><%= link_to glyph(:off) + " Salir", signout_path, method: "delete" %></li>
              </ul>
            </div>
          <% else %>  
            <li><%= link_to "Ingresar", signin_path  %></li>
          <% end %>
        </ul>

        <ul class="nav pull-right">
          <% if signed_in? %>
            <li><%= link_to "Encuestas", surveys_path  %></li>
            <% if signed_in? && current_user.admin? %>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Usuarios <b class="caret"></b>
                </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "Nuevo usuario", signup_path   %></li>
                    <li><%= link_to "Listar Usuarios", users_path  %></li>
                  </ul>
              </li>
            <% end %>
          <% end %>
        </ul>
    </div>
  </div>
</nav>