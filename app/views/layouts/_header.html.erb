<div class="navbar navbar-fluid-top">
  <div class="navbar-inner">
    <div class="container">
      <button type="button" class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="<%= root_path %>">Encuestas UM</a>
      <div class="container-fluid nav-collapse">
        <ul class="nav">
          <% if signed_in? %>
            <li class="active"><%= link_to "Encuestas", surveys_path  %></li>
          <% end %>
        </ul>
        <ul class="nav pull-right">
          <% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Cuenta <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Perfil", current_user %></li>
                <li><%= link_to "Actualizar Perfil", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li><%= link_to "Salir", signout_path, method: "delete" %></li>
              </ul>
            </li>
          <% else %>  
            <li><%= link_to "Ingresar", signin_path  %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>
