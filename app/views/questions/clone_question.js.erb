/*('#surveyTable tr:last').append('<tr><td>bla</td</tr>').fadeIn();*/


$("#questionTable").each(function () {
   
    var tds = '<tr>';
    $("#<%= dom_id(@question_old) %>").each(function () {
        tds += $(this).html();
    });
    tds += '</tr>';
    if ($('tbody', this).length > 0) {
        $('tbody', this).append(tds).effect("highlight", {}, 2000);
    } else {
        $(this).append(tds).effect("highlight", {}, 2000);
    }
});

/*$("#<%= dom_id(@survey) %>").fadeIn()*/
$(".alert").html("<%= escape_javascript(flash[:success])%>");
$(".alert").show(300);